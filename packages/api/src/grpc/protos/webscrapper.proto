syntax = "proto3";

package webscrapperPackage;

service WebScrapper {
  rpc ScrapArticle(Article) returns (ScrappedData) {};
}

message Article {
  string url = 1;
}

message ScrappedData {
  string data = 1;
}